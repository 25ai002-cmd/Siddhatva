services:
  - type: static
    name: siddhatva-builders
    buildCommand: "" # Pure static HTML site, no build command needed
    staticPublishPath: "." # Publish the root directory
    routes:
      - type: redirect
        source: /
        destination: /index.html
